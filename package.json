{
  "name": "youtube-analysis",
  "version": "1.0.0",
  "description": "Reusable Node.js module for fetching YouTube video metadata using yt-dlp",
  "main": "server/utils/fetchYouTubeDataForChannels.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "jest",
    "server": "nodemon server/server.js",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "youtube-dl-exec": "^2.5.8"
  }
}
